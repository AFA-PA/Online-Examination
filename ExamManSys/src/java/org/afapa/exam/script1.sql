CREATE TABLE OPTION (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE EXAM (ID BIGINT NOT NULL, DURATION LONGBLOB, NAME VARCHAR(255), STARTTIME TIME, ORGANIZATION_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE QUESTION (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE USER (ID BIGINT NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PHONENUMBER VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE ORGANIZATION (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE EXAM ADD CONSTRAINT FK_EXAM_ORGANIZATION_ID FOREIGN KEY (ORGANIZATION_ID) REFERENCES ORGANIZATION (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
